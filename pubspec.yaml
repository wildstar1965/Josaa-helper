name: teja
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0


  cupertino_icons: ^1.0.8
  provider: ^6.1.2   # FIXED — now in correct section

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
